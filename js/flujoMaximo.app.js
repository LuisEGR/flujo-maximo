var flujoMaximo = angular.module('flujoMaximoApp', ['ngMaterial', 'md.data.table']);
flujoMaximo.config(function($mdAriaProvider) {
   // Globally disables all ARIA warnings.
   $mdAriaProvider.disableWarnings();
});
